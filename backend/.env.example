# Required — server will not start without these
MONGO_URL=mongodb://localhost:27017
DB_NAME=scalemate

# Required for Studio song lookup + transposition (POST /api/process)
GROQ_API_KEY=

# Required for voice recording analysis (POST /api/analyze-audio) — add when wiring My Voice
GOOGLE_API_KEY=
